services:
  worker:
    build:
      context: .
      network: host
    environment:
      BPM_URL: ${BPM_URL}
      BPM_USER: ${BPM_USER}
      BPM_PASS: ${BPM_PASS}
    volumes:
      - ./session_cache:/app/session_cache
